AMS_lindberg_support = {
	AMS_find_loyalists = {

		complete_effect = {
			hidden_effect = {
				set_country_flag = AMS_support_actions
			}
		}
		available = {
			not = {
				has_country_flag = AMS_support_actions
			}
		}
		visible = {
			
		}
		cost = 25
		days_remove = 20
		days_re_enable = 30
		ai_will_do = {
			factor = 25
		}
		remove_effect = {
			hidden_effect = {
                add_to_variable = { AMS.lindberg_support = 2 }
				clr_country_flag = AMS_support_actions
            }
        }
	}
	AMS_incentives_as_bribes = {

		fire_only_once = yes
		complete_effect = {
			hidden_effect = {
				set_country_flag = AMS_support_actions
			}
		}
		available = {
			not = {
				has_country_flag = AMS_support_actions
			}
		}
		visible = {
			
		}
		cost = 50
		days_remove = 10
        days_re_enable = 30
		ai_will_do = {
			factor = 25
		}
		remove_effect = {
			hidden_effect = {
                add_to_variable = { AMS.lindberg_support = 3 }
				clr_country_flag = AMS_support_actions
            }
        }
	}
    AMS_reduce_military_influence = {

		complete_effect = {
			hidden_effect = {
				set_country_flag = AMS_support_actions
			}
		}
		available = {
			not = {
				has_country_flag = AMS_support_actions
			}
		}
		visible = {
			
		}
		cost = 30
		days_remove = 30
        days_re_enable = 40
		ai_will_do = {
			factor = 25
		}
		remove_effect = {
			hidden_effect = {
                add_to_variable = { AMS.lindberg_support = 2 }
                subtract_from_variable = { AMS.moseley_support = 1 }
				clr_country_flag = AMS_support_actions
            }
        }
	}
    AMS_speeches_for_american_people = {

		complete_effect = {
			hidden_effect = {
				set_country_flag = AMS_support_actions
			}
		}
		available = {
			not = {
				has_country_flag = AMS_support_actions
			}
		}
		visible = {
			
		}
		cost = 25
		days_remove = 50
        days_re_enable = 60
		ai_will_do = {
			factor = 25
		}
		remove_effect = {
			hidden_effect = {
                add_to_variable = { AMS.lindberg_support = 2 }
				clr_country_flag = AMS_support_actions
            }
        }
	}
	AMS_power_struggle_lindberg = {

        allowed = { tag = AMS }

        available = {
            has_country_flag = AMS_lindberg_power
        }

        activation = {
            has_country_flag = AMS_lindberg_choice
        }

        is_good = no

        selectable_mission = no

        days_mission_timeout = 365

        timeout_effect = {
            clr_country_flag = AMS_lindberg_choice
			set_country_flag = AMS_effinger_time
        }

        complete_effect = {
			clr_country_flag = AMS_lindberg_choice
        }
    }
	AMS_moseley_moves_lindberg = {

        allowed = { tag = AMS }

        available = {
            not = {
                has_country_flag = AMS_lindberg_choice
            }
        }

        activation = {
            has_country_flag = AMS_lindberg_choice
        }

        is_good = no

        selectable_mission = no

        days_mission_timeout = 20

        timeout_effect = {
			add_to_variable = { AMS.moseley_support = 2 }
        }

        complete_effect = {
			
        }
    }
	AMS_thurmond_moves_lindberg = {

        allowed = { tag = AMS }

        available = {
            not = {
                has_country_flag = AMS_lindberg_choice
            }
        }

        activation = {
            has_country_flag = AMS_lindberg_choice
        }

        is_good = no

        selectable_mission = no

        days_mission_timeout = 20

        timeout_effect = {
			add_to_variable = { AMS.thurmond_support = 2 }
        }

        complete_effect = {
			
        }
    }
}
AMS_moseley_support = {
	AMS_gather_military_support = {

		complete_effect = {
			hidden_effect = {
				set_country_flag = AMS_support_actions
			}
		}
		available = {
			not = {
				has_country_flag = AMS_support_actions
			}
		}
		visible = {
			
		}
		cost = 30
		days_remove = 30
		days_re_enable = 40
		ai_will_do = {
			factor = 25
		}
		remove_effect = {
			hidden_effect = {
                add_to_variable = { AMS.moseley_support = 2 }
				clr_country_flag = AMS_support_actions
            }
        }
	}
	AMS_replace_security_guards = {

		fire_only_once = yes
		complete_effect = {
			hidden_effect = {
				set_country_flag = AMS_support_actions
			}
		}
        fire_only_once = yes
		available = {
			not = {
				has_country_flag = AMS_support_actions
			}
		}
		visible = {
			
		}
		cost = 25
		days_remove = 20
        days_re_enable = 30
		ai_will_do = {
			factor = 25
		}
		remove_effect = {
			hidden_effect = {
                add_to_variable = { AMS.moseley_support = 3 }
				clr_country_flag = AMS_support_actions
            }
        }
	}
    AMS_race_restrictions_military = {

		complete_effect = {
			hidden_effect = {
				set_country_flag = AMS_support_actions
			}
		}
        fire_only_once = yes
		available = {
			not = {
				has_country_flag = AMS_support_actions
			}
		}
		visible = {
			
		}
		cost = 40
		days_remove = 40
        days_re_enable = 45
		ai_will_do = {
			factor = 25
		}
		remove_effect = {
			hidden_effect = {
                add_to_variable = { AMS.moseley_support = 3 }
				clr_country_flag = AMS_support_actions
            }
        }
	}
    AMS_advocate_for_mil_growth = {

		complete_effect = {
			hidden_effect = {
				set_country_flag = AMS_support_actions
			}
		}
		available = {
			not = {
				has_country_flag = AMS_support_actions
			}
		}
		visible = {
			
		}
		cost = 30
		days_remove = 10
        days_re_enable = 30
		ai_will_do = {
			factor = 25
		}
		remove_effect = {
			hidden_effect = {
                add_to_variable = { AMS.moseley_support = 2 }
				clr_country_flag = AMS_support_actions
            }
        }
	}
	AMS_power_struggle_moseley = {

        allowed = { tag = AMS }

        available = {
            has_country_flag = AMS_moseley_power
        }

        activation = {
            has_country_flag = AMS_moseley_choice
        }

        is_good = no

        selectable_mission = no

        days_mission_timeout = 365

        timeout_effect = {
            clr_country_flag = AMS_moseley_choice
			set_country_flag = AMS_effinger_time
        }

        complete_effect = {
			clr_country_flag = AMS_moseley_choice
        }
    }
	AMS_lindberg_moves_moseley = {

        allowed = { tag = AMS }

        available = {
            not = {
                has_country_flag = AMS_moseley_choice
            }
        }

        activation = {
            has_country_flag = AMS_moseley_choice
        }

        is_good = no

        selectable_mission = no

        days_mission_timeout = 20

        timeout_effect = {
			add_to_variable = { AMS.lindberg_support = 2 }
        }

        complete_effect = {
			
        }
    }
	AMS_thurmond_moves_moseley = {

        allowed = { tag = AMS }

        available = {
            not = {
                has_country_flag = AMS_moseley_choice
            }
        }

        activation = {
            has_country_flag = AMS_moseley_choice
        }

        is_good = no

        selectable_mission = no

        days_mission_timeout = 20

        timeout_effect = {
			add_to_variable = { AMS.thurmond_support = 2 }
        }

        complete_effect = {
			
        }
    }
}
AMS_thurmond_support = {
	AMS_call_out_opponents = {

		complete_effect = {
			hidden_effect = {
				set_country_flag = AMS_support_actions
			}
		}
		available = {
			not = {
				has_country_flag = AMS_support_actions
			}
		}
		visible = {
			
		}
		cost = 20
		days_remove = 20
		days_re_enable = 30
		ai_will_do = {
			factor = 25
		}
		remove_effect = {
			hidden_effect = {
                add_to_variable = { AMS.thurmond_support = 2 }
				clr_country_flag = AMS_support_actions
            }
        }
	}
	AMS_reduce_lindberg_connection = {

		fire_only_once = yes
		complete_effect = {
			hidden_effect = {
				set_country_flag = AMS_support_actions
			}
		}
		available = {
			not = {
				has_country_flag = AMS_support_actions
			}
		}
		visible = {
			
		}
		cost = 50
		days_remove = 30
        days_re_enable = 40
		ai_will_do = {
			factor = 25
		}
		remove_effect = {
			hidden_effect = {
                add_to_variable = { AMS.thurmond_support = 3 }
                subtract_from_variable = { AMS.lindberg_support = 1}
				clr_country_flag = AMS_support_actions
            }
        }
	}
    AMS_lead_segregation_law = {

		complete_effect = {
			hidden_effect = {
				set_country_flag = AMS_support_actions
			}
		}
		available = {
			not = {
				has_country_flag = AMS_support_actions
			}
		}
		visible = {
			
		}
		cost = 30
		days_remove = 35
        days_re_enable = 40
		ai_will_do = {
			factor = 25
		}
		remove_effect = {
			hidden_effect = {
                add_to_variable = { AMS.thurmond_support = 2 }
				clr_country_flag = AMS_support_actions
            }
        }
	}
    AMS_blame_moseley_defeat = {

		complete_effect = {
			hidden_effect = {
				set_country_flag = AMS_support_actions
			}
		}
		available = {
			not = {
				has_country_flag = AMS_support_actions
			}
		}
		visible = {
			
		}
		cost = 35
		days_remove = 40
        days_re_enable = 30
		ai_will_do = {
			factor = 25
		}
		remove_effect = {
			hidden_effect = {
                add_to_variable = { AMS.thurmond_support = 2 }
                subtract_from_variable = { AMS.moseley_support = 1 }
				clr_country_flag = AMS_support_actions
            }
        }
	}
	AMS_power_struggle_thurmond = {

        allowed = { tag = AMS }

        available = {
            has_country_flag = AMS_thurmond_power
        }

        activation = {
            has_country_flag = AMS_thurmond_choice
        }

        is_good = no

        selectable_mission = no

        days_mission_timeout = 365

        timeout_effect = {
            clr_country_flag = AMS_thurmond_choice
			set_country_flag = AMS_effinger_time
        }

        complete_effect = {
			clr_country_flag = AMS_thurmond_choice
        }
    }
	AMS_lindberg_moves_thurmond = {

        allowed = { tag = AMS }

        available = {
            not = {
                has_country_flag = AMS_thurmond_choice
            }
        }

        activation = {
            has_country_flag = AMS_thurmond_choice
        }

        is_good = no

        selectable_mission = no

        days_mission_timeout = 20

        timeout_effect = {
			add_to_variable = { AMS.lindberg_support = 2 }
        }

        complete_effect = {
			
        }
    }
	AMS_moseley_moves_thurmond = {

        allowed = { tag = AMS }

        available = {
            not = {
                has_country_flag = AMS_thurmond_choice
            }
        }

        activation = {
            has_country_flag = AMS_thurmond_choice
        }

        is_good = no

        selectable_mission = no

        days_mission_timeout = 20

        timeout_effect = {
			add_to_variable = { AMS.moseley_support = 2 }
        }

        complete_effect = {
			
        }
    }
}